{
  "name": "workspace",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "build": "pnpm -r run build"
  },
  "devDependencies": {
    "typescript": "~5.9.3"
  }
}